[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short -m "not rag_eval"
markers =
    rag_eval: 慢速 RAG 评估（默认被 addopts 跳过；手动 pytest -m rag_eval 或 pytest -m "not rag_eval" 关闭后跑）