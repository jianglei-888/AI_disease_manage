# 微信小程序架构文档

## 1. 技术栈选型

| 技术 | 版本 | 用途 |
|------|------|------|
| 微信小程序 | 最新 | 前端框架 |
| WXML | 最新 | 页面结构 |
| WXSS | 最新 | 页面样式 |
| JavaScript | ES6+ | 逻辑处理 |
| wx.request | 最新 | 网络请求 |
| wx.storage | 最新 | 本地存储 |

> 当前版本未接入 `wx.login` 微信原生登录，仅支持邮箱注册 / 登录。Roadmap 见 [AI_disease_backend/README.md](../AI_disease_backend/README.md#roadmap)。

## 2. 目录结构

```
AI_disease_front/
├── pages/
│   ├── login/               # 登录
│   ├── register/            # 注册
│   ├── home/                # 首页（tabBar）
│   ├── submit/              # 提交数据（tabBar）
│   ├── chat/                # 聊天（tabBar）
│   ├── profile/             # 个人中心（tabBar）
│   ├── analysis/            # 周分析（非 tabBar）
│   └── personal-info/       # 个人信息（非 tabBar）
├── components/              # 自定义组件
│   ├── health-card/
│   ├── advice-card/
│   └── message-bubble/
├── images/                  # 静态图片资源
├── utils/
│   ├── api.js               # API 调用封装（统一 wx.request）
│   ├── auth.js              # token 存取
│   ├── storage.js           # wx.storage 封装
│   └── common.js            # 时间格式化 / Toast / 邮箱校验
├── services/                # 业务封装
│   ├── auth.service.js
│   ├── health.service.js
│   └── chat.service.js
├── app.json                 # 小程序配置（tabBar：home / submit / chat / profile）
├── app.js                   # 小程序入口 + globalData
├── app.wxss                 # 全局样式
└── project.config.json      # 项目配置
```

## 3. 页面设计

### 3.1 登录页

- **路径**: `pages/login/login.wxml`
- **功能**:
  - 邮箱输入框
  - 密码输入框
  - 记住密码选项
  - 登录按钮
  - 忘记密码链接
  - 注册链接

### 3.2 注册页

- **路径**: `pages/register/register.wxml`
- **功能**:
  - 用户名输入框
  - 邮箱输入框
  - 密码输入框
  - 确认密码输入框
  - 注册按钮
  - 登录链接

### 3.3 首页

- **路径**: `pages/home/home.wxml`
- **功能**:
  - 今日健康数据卡片
  - 饮食建议卡片
  - 运动建议卡片
  - 快速添加血压血糖记录按钮
  - 健康咨询按钮
  - 底部 Tabbar 导航

### 3.4 提交数据页

- **路径**: `pages/submit/submit.wxml`
- **功能**:
  - 血压输入框（高压 / 低压）
  - 血糖输入框
  - 测量时间选择器
  - 提交按钮

### 3.5 聊天页

- **路径**: `pages/chat/chat.wxml`
- **功能**:
  - 聊天消息列表
  - 消息输入框
  - 发送按钮
  - 聊天历史记录

### 3.6 个人中心页

- **路径**: `pages/profile/profile.wxml`
- **功能**:
  - 用户信息展示
  - 账号设置
  - 历史记录
  - 通知设置
  - 免责声明
  - 隐私政策
  - 关于我们
  - 退出登录按钮

## 4. 组件设计

### 4.1 健康数据卡片组件

- **路径**: `components/health-card/health-card.wxml`
- **功能**:
  - 显示血压和血糖数据
  - 数据状态显示
  - 查看历史记录入口

### 4.2 建议卡片组件

- **路径**: `components/advice-card/advice-card.wxml`
- **功能**:
  - 显示建议标题
  - 显示建议内容
  - 图标展示

### 4.3 消息气泡组件

- **路径**: `components/message-bubble/message-bubble.wxml`
- **功能**:
  - 显示用户消息
  - 显示 AI 消息
  - 时间戳显示

## 5. API 调用设计

### 5.1 API 基础配置

```javascript
// utils/api.js
// 开发期走本机后端：'http://127.0.0.1:8001'（与后端 .env 的 APP_PORT 一致）
// 真机调试（小程序连后端）：改成电脑局域网 IP，如 'http://192.168.1.100:8001'
const API_BASE_URL = 'http://127.0.0.1:8001';

function request(options) {
  return new Promise((resolve, reject) => {
    wx.request({
      url: API_BASE_URL + options.url,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Content-Type': 'application/json',
        'Authorization': 'Bearer ' + wx.getStorageSync('token')
      },
      success: (res) => {
        if (res.statusCode === 200) {
          resolve(res.data);
        } else {
          reject(res.data);
        }
      },
      fail: (err) => {
        reject(err);
      }
    });
  });
}

export default {
  request
};
```

### 5.2 认证服务

```javascript
// services/auth.service.js
import api from '../utils/api';

export default {
  // 注册
  register(data) {
    return api.request({
      url: '/api/auth/register',
      method: 'POST',
      data
    });
  },

  // 登录
  login(data) {
    return api.request({
      url: '/api/auth/login',
      method: 'POST',
      data
    });
  },

  // 获取用户信息
  getMe() {
    return api.request({
      url: '/api/auth/me',
      method: 'GET'
    });
  }
};
```

### 5.3 健康数据服务

```javascript
// services/health.service.js
import api from '../utils/api';

export default {
  // 提交健康数据
  submitRecord(data) {
    return api.request({
      url: '/api/health/records',
      method: 'POST',
      data
    });
  },

  // 获取历史数据
  getRecords(params) {
    return api.request({
      url: '/api/health/records',
      method: 'GET',
      data: params
    });
  },

  // 获取数据分析
  getAnalysis(params) {
    return api.request({
      url: '/api/health/analysis',
      method: 'GET',
      data: params
    });
  }
};
```

### 5.4 聊天服务

```javascript
// services/chat.service.js
import api from '../utils/api';

export default {
  // 发送消息
  sendMessage(data) {
    return api.request({
      url: '/api/chat/messages',
      method: 'POST',
      data
    });
  },

  // 获取聊天历史
  getMessages(params) {
    return api.request({
      url: '/api/chat/messages',
      method: 'GET',
      data: params
    });
  }
};
```

## 6. 状态管理

### 6.1 全局状态

```javascript
// app.js
App({
  globalData: {
    userInfo: null,
    token: null,
    isLoggedIn: false
  },

  onLaunch() {
    // 检查登录状态
    const token = wx.getStorageSync('token');
    if (token) {
      this.globalData.token = token;
      this.globalData.isLoggedIn = true;
      // 获取用户信息
      this.getUserInfo();
    }
  },

  getUserInfo() {
    // 调用 API 获取用户信息
    // ...
  },

  login(token, userInfo) {
    this.globalData.token = token;
    this.globalData.userInfo = userInfo;
    this.globalData.isLoggedIn = true;
    wx.setStorageSync('token', token);
  },

  logout() {
    this.globalData.token = null;
    this.globalData.userInfo = null;
    this.globalData.isLoggedIn = false;
    wx.removeStorageSync('token');
  }
});
```

### 6.2 页面状态

每个页面维护自己的局部状态，如表单数据、加载状态等。

## 7. 路由设计

### 7.1 页面路由

| 页面 | 路径 | 功能 |
|------|------|------|
| 登录页 | `/pages/login/login` | 用户登录 |
| 注册页 | `/pages/register/register` | 用户注册 |
| 首页 | `/pages/home/home` | 主页，显示健康数据和建议 |
| 提交数据页 | `/pages/submit/submit` | 提交血压血糖数据 |
| 聊天页 | `/pages/chat/chat` | 与 AI 进行健康咨询 |
| 个人中心页 | `/pages/profile/profile` | 用户个人信息和设置 |

### 7.2 导航方式

- **tabBar 导航**: 首页、提交、聊天、个人中心
- **navigateTo**: 跳转到非 tabBar 页面
- **redirectTo**: 重定向到其他页面
- **navigateBack**: 返回上一页

## 8. 数据存储

### 8.1 本地存储

- `token`: 存储用户认证令牌
- `userInfo`: 存储用户信息
- `lastLoginTime`: 存储上次登录时间
- `formData`: 存储表单数据草稿

### 8.2 存储操作

```javascript
// utils/storage.js
export default {
  // 存储数据
  set(key, value) {
    wx.setStorageSync(key, value);
  },

  // 获取数据
  get(key) {
    return wx.getStorageSync(key);
  },

  // 删除数据
  remove(key) {
    wx.removeStorageSync(key);
  },

  // 清空所有数据
  clear() {
    wx.clearStorageSync();
  }
};
```

## 9. 安全措施

1. **数据加密**: 敏感数据传输加密
2. **Token 管理**: 合理管理 JWT 令牌
3. **输入验证**: 前端输入验证
4. **权限控制**: 确保用户只能访问自己的数据
5. **错误处理**: 合理处理错误，不暴露敏感信息
6. **网络安全**: 生产环境使用 HTTPS 协议

## 10. 性能优化

1. **代码分包**: 减小主包体积
2. **图片优化**: 压缩图片，使用合适的格式
3. **缓存策略**: 合理使用缓存
4. **减少请求**: 合并请求，使用批量接口
5. **懒加载**: 按需加载组件和数据
6. **渲染优化**: 减少不必要的渲染

## 11. 兼容性处理

1. **设备兼容性**: 适配不同屏幕尺寸
2. **系统兼容性**: 适配不同微信版本
3. **网络兼容性**: 处理网络异常情况
4. **数据兼容性**: 处理不同格式的数据

## 12. 测试策略

1. **功能测试**: 测试各功能模块
2. **兼容性测试**: 测试不同设备和微信版本
3. **性能测试**: 测试页面加载速度和响应时间
4. **安全测试**: 测试数据安全和权限控制
5. **用户体验测试**: 测试用户操作流程

## 13. 发布流程

1. **代码审核**: 确保代码符合微信小程序规范
2. **功能测试**: 测试所有功能
3. **性能优化**: 优化小程序性能
4. **提交审核**: 提交到微信公众平台审核
5. **发布上线**: 审核通过后发布
6. **版本管理**: 管理小程序版本

## 14. 维护与更新

1. **Bug 修复**: 及时修复发现的问题
2. **功能更新**: 根据用户反馈添加新功能
3. **性能优化**: 持续优化小程序性能
4. **安全更新**: 及时更新安全相关组件
5. **用户反馈**: 收集和处理用户反馈
